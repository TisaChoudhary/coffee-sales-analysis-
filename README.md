# Coffee Sales Analysis Project

This project analyzes coffee sales data from a vending machine.

## Requirements

Install the required packages using:

pip install -r requirements.txt

## Running the Analysis

Run the main script:

python main.py

This will perform the data cleaning, EDA, and generate various plots for analysis. The plots will be saved in the Visualizations/ folder.
